class Background: RscText {
    idc = -1;

    x = QUOTE(0 * GRID_W);
    y = QUOTE(0 * GRID_H);
    w = QUOTE(UPPERHUD_W * GRID_W);
    h = QUOTE(UPPERHUD_H * GRID_H);

    // colorBackground[] = {1,1,1,1};
};