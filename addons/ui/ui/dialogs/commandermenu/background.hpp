class Background : IGUIBACK
{
    x = QUOTE(COMMANDERMENU_X * GRID_W + CENTER_X);
    y = QUOTE(COMMANDERMENU_Y * GRID_H + CENTER_Y);
    w = QUOTE(COMMANDERMENU_W * GRID_W);
    h = QUOTE(COMMANDERMENU_H * GRID_H);

    colorBackground[] = {0,0,0,0.75};
};
